name: 每日AI新闻简报（Minimax）

on:
  schedule:
    - cron: '0 0 * * *'  # 北京时间 每天 8:00
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 安装工具
        run: sudo apt-get update && sudo apt-get install -y curl jq

      - name: 获取AI新闻
        id: get_news
        run: |
          API_KEY="sk-api-g_fL1Sz_Ht6gWJxqdvlMkCzkzfg0HrJCjHmBxXHs5y8je5R2Hz9TD9FoGhFvKLSv-rKuq5gJsDwwzcuSF4O5eWbdEQ635tkM7pgVi7QAybca9tIsW9XsPmw"
          GROUP_ID="2025760618228753282"

          echo "正在调用 Minimax API..."
          RESULT=$(curl -s "https://api.minimax.chat/v1/text/chatcompletion?GroupId=$GROUP_ID" \
            -H "Authorization: Bearer $API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "model": "MiniMax-M2.5",
              "messages": [
                {
                  "role": "user",
                  "content": "你是贾维斯。请生成今天最新的AI行业简报，只包含Agent、MCP、大模型，每条标注日期，总共5条，简洁专业，不要多余格式。"
                }
              ],
              "temperature": 0.2
            }')

          echo "API 原始响应: $RESULT"

          if echo "$RESULT" | jq -e '.data.reply' > /dev/null; then
            CONTENT=$(echo "$RESULT" | jq -r '.data.reply')
          else
            CONTENT="API 调用失败，返回内容: $RESULT"
          fi

          echo "content=$CONTENT" >> $GITHUB_OUTPUT

      - name: 输出内容（自动发GitHub邮件）
        run: echo "${{ steps.get_news.outputs.content }}"
